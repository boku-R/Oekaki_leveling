<div class="container-fluid background_beige py-3">
  <div class="row">
    <div class="mx-auto">
      <%= render "topbar",user: @user %>
    </div>
  </div>
</div>

<div class="container-fluid py-2">
  <%= flash[:notice] %>
  <% if @user.errors.any? %>
    <p><%= @user.errors.count %>errors prohibited this obj from being saved:</p>
    <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
  <div class="row mt-4">
    <div class="col-md-3">
      <%= render "sidebar",user: @user %>
    </div>

    <div class="col-md-9">
      <div class="row">
        <div class="col-md-5 d-flex justify-content-center">
          <%= image_tag @user.get_profile_image(200,200),class: "mr-3 rounded-circle" %>
        </div>

        <div class="col-md-7 d-block">
          <p class="h4"><%= @user.handlename %></p>
          <p>@<%= @user.username %></p>
          <% if @user == current_user %>
            <p><i class="fas fa-envelope"></i> <%= @user.email %></p>
          <% end %>
          <p><i class="fas fa-palette"></i> Lv.<%= @user.level %></p>
          <p>下書き<i class="fas fa-heart"></i><%= @draft_favorite_count %>    線画<i class="fas fa-heart"></i><%= @linedraw_favorite_count %>   色塗り<i class="fas fa-heart"></i><%= @color_favorite_count %>    完成<i class="fas fa-heart"></i><%= @finish_favorite_count %></p>
        </div>
      </div>
      <div class="row">
        <div class="col mt-3">
          <%= render "public/posts/index",title: "投稿一覧", posts: @posts %>
        </div>
      </div>
    </div>
  </div>
</div>